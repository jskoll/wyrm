/wyrm
dist/
coverage.out
.DS_Store
site/
.venv-docs/
# wyrm's own dogfood session config; not part of the shipped project.
.wyrm.toml

# Release signing: the public half is committed at
# internal/selfupdate/signing.pub and nothing else about the key belongs here.
*.key
minisign.key
*.minisig
