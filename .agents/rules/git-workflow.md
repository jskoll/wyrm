# Git Workflow Rule

- **Never work directly on the `main` branch.**
- Always create a dedicated feature or bugfix branch based on `origin/main` before making code changes or commits.
- Ensure `main` remains clean and strictly tracks `origin/main`.
