version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - revive
    - gocritic
  exclusions:
    rules:
      # Tests may ignore cleanup errors freely
      - path: _test\.go
        linters:
          - errcheck
