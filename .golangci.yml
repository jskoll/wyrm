linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - revive
    - gocritic

issues:
  exclude-rules:
    # Tests may ignore cleanup errors freely
    - path: _test\.go
      linters:
        - errcheck
